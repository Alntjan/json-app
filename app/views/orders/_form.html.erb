<%= form_for(order) do |f| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :shop_id %>
    <%= f.number_field :shop_id %>
  </div>

  <div class="field">
    <%= f.label :shopify_order_id %>
    <%= f.text_field :shopify_order_id %>
  </div>

  <div class="field">
    <%= f.label :order_data %>
    <%= f.text_field :order_data %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
